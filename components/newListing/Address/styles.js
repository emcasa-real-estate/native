import {StyleSheet} from 'react-native'

export default StyleSheet.create({
  container: {
    margin: 20
  },
  autoComplete: {
    height: 60,
    overflow: 'visible',
    zIndex: 1
  },
  field: {
    overflow: 'visible',
    height: 50,
    marginBottom: 15
  }
})
