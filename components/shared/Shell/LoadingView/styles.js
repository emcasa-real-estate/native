import {StyleSheet} from 'react-native'

export default new StyleSheet.create({
  container: {flex: 1},
  indicatorContainer: {
    flex: 1,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  }
})
